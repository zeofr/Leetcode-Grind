# 1484. Group Sold Products By The Date

## 💡 My Approach
Main Concept : `GROUP_CONCAT()`

The syntax is:

```
GROUP_CONCAT(
    [DISTINCT] expression
    ORDER BY expression
    SEPARATOR 'delimiter'
)
```

---

_Official problem description unavailable._